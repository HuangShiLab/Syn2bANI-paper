# Python dependencies for Syn2bANI benchmarking pipeline
# Install: pip install -r scripts/requirements.txt

# Data processing
pandas>=2.0
numpy>=1.24

# Visualization
matplotlib>=3.7
seaborn>=0.12

# ML
scikit-learn>=1.3

# Optional but recommended
# seqkit      # install via conda: conda install -c bioconda seqkit
# skani       # install via conda: conda install -c bioconda skani
# fastani     # install via conda: conda install -c bioconda fastani
