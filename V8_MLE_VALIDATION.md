# v8：chain-restricted stratified MLE — 算法与验证记录

> 日期：2026-07-25
> 代码：Syn2bANI `3048177`，子命令 `syn2bani ani`
> 设计文档：代码仓库根目录 `ALGORITHM_MLE.md`（含完整推导）

---

## 1. 为什么放弃 GBRT 校准路线

不是调参问题，是建模问题。三条独立原因：

### 1.1 `raw_ani` 被 mismatch 预算钉死

`local_ani = 1 − hamming / tag_len` 在 `near_match_tolerance = 2`、32 bp tag 下
只能取 `{1.0, 0.969, 0.938}`，所以 `mean(local_ani) ≥ 1 − 2/32 = 0.9375`，
与两个基因组多分歧无关。

实测它对已知真值的**斜率只有 0.336**（85–99.9% ANI 区间）：信号被压缩约 3 倍，
反解时噪声被放大 3 倍。而 GBRT v7 给这个特征的重要性最高（0.3197）。

### 1.2 全基因组 containment 把分歧和共享含量混在一起

```
C_genome ≈ (1 − F) · ANI^k        F = accessory / 非同源比例
```

所以 `C^(1/k)` 返回 `ANI · (1−F)^(1/k)`，偏差大小取决于 `F`，而 `F` 每一对都不同。

**关键推论**：在 `1 + ln(C)/k` 上加一个常数偏移，代数上等价于给所有基因组对
硬编码同一个 `F`。它在共享含量相近的验证集上"有效"，在共享含量变化时必然失效。
这正是 `−0.028` 经验校准在做的事情。

### 1.3 多酶混用单一 `k` 有系统偏差

酶 panel 的 `tag_length` 从 25（BslFI）到 33（CspCI）。98% ANI 时各酶精确匹配
存留率跨 0.51–0.60。混池后用 `k = 32` 反解，98% 处偏高约 0.18 个百分点、
99.5% 处约 0.04 个百分点——绝对值不大，但在菌株分辨率上是决定性的，而且
**换酶组合时会漂移**。

### 1.4 真正的关系

`raw_ani` 和 `mash_ani` 不是两个独立特征，而是**同一个似然的两个矩**：
mismatch 直方图和丢失率由同一个每碱基分歧度生成。拟合这个似然就不需要学习组合，
也不会退化——直方图饱和时，权重自动转移到丢失率上，由 Fisher information
决定，而不是由训练数据决定。

---

## 2. 估计量

Type IIB tag 内部含识别位点，所以 query tag 存在的前提是该位点在 query 中完好。
链内一个 query tag，令 `k` = tag 长度、`s` = 识别位点长度、`b = k − s` = 可变体、
`tol` = mismatch 预算：

```
被找到、body 有 m 个 mismatch    P_m(a) = C(b, m) · (1−a)^m · a^(k−m)    m ≤ tol
未找到                            P_miss(a) = 1 − Σ_{m≤tol} P_m(a)
```

mismatch 只能在 body 上被观测到：位点上的突变直接把 tag 从那个基因组里删除。
所以位点贡献 `a^s`（已折进 `a^(k−m)`）但永远不会表现为观测到的 mismatch。
这也给出一个硬上限——**任何 mismatch 预算都无法保留超过 `a^s` 的 tag**，
95% ANI 时约 0.72，90% 时约 0.50。

对数似然按酶分层求和（每个酶用自己的 `k`、`b`），对单个标量 `a` 最大化。
一维、光滑，golden-section 收敛。

**计数必须来自链内**：这从构造上把 accessory 排除在分母之外，于是 ANI 只度量分歧、
AF 单独度量共享含量。这是对 §1.2 的结构性修复。

**内建质控**：`ani_from_loss`（仅丢失率）和 `ani_from_hist`（仅直方图，按
`P(found)` 重归一化）是同一个量的两个独立估计。差异超过约 5 个标准误说明模型假设
被违反（重复序列、污染、链拉错），标记 `INCONSISTENT`。该检查在期望存留率
< 0.20 时自动关闭：低于该值时存活的直方图只是被截断的尾部，不再是独立估计量。

---

## 3. 验证：精确已知 ground truth

真值由构造给出：对参考基因组施加计数好的替换突变，
所以 true ANI = `1 − n_subs / length`。工具在代码仓库 `prototype/`，
每套仿真约 90 MB（生成物不入库），**不需要 GTDB**。

参考基因组固定为公开 accession——*E. coli* K-12 MG1655，ENA `U00096.3`，
4,641,652 bp——这样任何人都从逐字节相同的输入出发仿真，下面的 MAE 可以直接比较
而不是近似比较。（初版数字用的是一个本地截断到 4,543,028 bp 的副本，
无法从公开来源复现，已弃用。）

```bash
cd Syn2bANI/prototype
bash fetch_reference.sh mg1655.fasta
python3 simulate.py mg1655.fasta sim
python3 simulate_accessory.py mg1655.fasta simacc 0.95
../target/release/syn2bani ani sim/q_*.fasta sim/ref.fasta --verbose -p
../target/release/syn2bani ani simacc/acc*.fasta simacc/ref.fasta --verbose -p
```

### 3.1 ANI 梯度（12 个基因组，85 → 99.9%，含 400 kb 倒位，无 accessory）

| true ANI | 估计 | 误差 | AF |
|---|---|---|---|
| 85.000 | 85.011 | +0.011 | 0.987 |
| 88.000 | 88.181 | +0.181 | 0.995 |
| 90.000 | 89.991 | −0.009 | 0.995 |
| 92.000 | 92.156 | +0.156 | 0.997 |
| 94.000 | 94.009 | +0.009 | 0.999 |
| 95.000 | 94.996 | −0.004 | 0.998 |
| 96.000 | 96.046 | +0.046 | 0.999 |
| 97.000 | 97.073 | +0.073 | 0.999 |
| 98.000 | 97.982 | −0.018 | 0.999 |
| 99.000 | 98.932 | −0.069 | 0.999 |
| 99.500 | 99.472 | −0.028 | 0.999 |
| 99.900 | 99.867 | −0.033 | 0.999 |

**MAE 0.053%**，零训练数据、零校准模型、零经验偏移。全部 12 行 `flag=ok`。

### 3.2 accessory 混淆（6 个基因组，true ANI 固定 95.000，accessory 0 → 50%）

| accessory | 估计 | 误差 | AF | chains |
|---|---|---|---|---|
| 0%  | 95.044 | +0.044 | 1.000 | 1 |
| 10% | 95.106 | +0.106 | 0.896 | 6 |
| 20% | 95.074 | +0.074 | 0.796 | 6 |
| 30% | 95.071 | +0.071 | 0.698 | 6 |
| 40% | 95.250 | +0.250 | 0.597 | 6 |
| 50% | 95.140 | +0.140 | 0.496 | 6 |

**MAE 0.114%。** 估计值保持平坦——残差是散布加一个约 +0.1 的均匀偏置，
**不随 accessory 比例单调变化**——而 AF 精确跟踪 `1 − F`（误差 ≤ 0.004）。
两条信号解耦。作为对照，同一批基因组上全基因组 containment 从 95.18 漂移到
93.27——**这个漂移正是 GBRT 一直在通过 `af_q` 隐式反推的东西**。

### 3.3 分块数对照实验（6 个基因组，true ANI 95.000，accessory 固定 20%）

固定 accessory **比例**，只改分成几块——所以总非同源含量和 true ANI 完全相同，
只有链的几何形状变了。这是暴露 `max_gap` 缺陷的那个实验。

| 块数 | 块大小 | 估计 | 误差 | AF（真值 0.800） | chains |
|---|---|---|---|---|---|
| 1  | 928 kb | 95.041 | +0.041 | 0.799 | 2 |
| 2  | 464 kb | 94.966 | −0.034 | 0.799 | 3 |
| 5  | 186 kb | 95.040 | +0.040 | 0.797 | 6 |
| 10 | 93 kb  | 94.992 | −0.008 | 0.796 | 11 |
| 20 | 46 kb  | 95.066 | +0.066 | 0.789 | 21 |
| 40 | 23 kb  | 95.117 | +0.117 | 0.783 | 41 |

**MAE 0.051%。** 修复前最后两行是 −0.739（AF 0.956，5 条链）和
−0.901（AF 1.000，1 条链）——链把每个 accessory 块都跨过去了。

**这个实验还顺便证伪了一个假设**：偏置**不随链数增长**，所以 §3.2 里残留的
约 +0.1 不是链边界删失造成的（原假设是链端点按定义是匹配成功的 anchor，
所以端点外的失败 tag 会被整体丢弃）。那个残差来源仍未查明。

### 3.4 关键发现：bp 形式的链断裂判据在原理上不可行

这是本轮最重要的算法发现，**论文里应该单独讲**。

一段连续锚不上的 tag 到底意味着"分歧但同源"还是"不同源"，取决于每个 tag 的
锚定概率 `p`，而 `p` 取决于我们正要估计的分歧度。95% ANI、`tol=2` 时
`p ≈ 0.79`，连续失败 9 个就已不可能（约 6 kb）；85% ANI 时 `p ≈ 0.12`，
连续失败 100 个是家常便饭（约 78 kb）。实测没有任何固定 `max_gap` 能两头兼顾：

| `max_gap` | §3.1 MAE | 85% ANI 处误差 | 46 kb accessory 块 |
|---|---|---|---|
| 50 kb | 0.053% | +0.011（AF 0.987） | **−0.739**（AF 0.956） |
| 20 kb | 0.152% | +1.059（AF 0.707） | +0.066（AF 0.789） |
| 10 kb | 0.430% | **+3.071**（AF 0.282） | +0.070（AF 0.787） |

太宽松→链跨过非同源区，把其 tag 拉进分母，ANI 偏低、AF 虚高；
太严格→链碎裂到 `min_chain_anchors` 以下，anchor 稀疏的区段被整体丢弃，
ANI 偏高、AF 偏低。

**解法：判据改为数"跳过了多少个 query tag 位点"，阈值由拟合出的分歧度决定。**
这个形式无标度，而且天然区分 bp 判据混为一谈的两件事：

- **缺失**删掉的是 reference 的 tag，存活的 query tag 依然相邻——跳过 0 个位点，
  不断链（偏移量不匹配由 gap penalty 处理）；
- **等长非同源替换**（重组、HGT 置换）会跳过其内部**每一个** query tag 位点——断链。

阈值 `j* = ln(α)/ln(1−p)`，`α` 默认 1e-6，下限取 5（重复屏蔽的 tag 和酶 panel
覆盖空隙会占用 tag 位点但不是 anchor，否则近乎相同的基因组会被切碎）。
第一趟宽松建链并拟合，第二趟按 `j*` 重建再拟合。

**对真实数据的意义**：真实的 accessory 区段——一个 10 kb 前噬菌体、
一个 20 kb 基因组岛——基本都远小于 50 kb。所以旧的固定 `max_gap` 会在真实基因组上
把大部分 accessory 吞进链里，系统性压低 ANI。这很可能是真实数据表现不及预期的
一大来源，值得在 Task 0 里专门核对 AF 列。

### 3.5 与既有数字的关系

⚠️ **不是同一把尺子，不能直接比较。** 下表仅供定位：

| 方法 | MAE | 真值来源 | 数据 |
|---|---|---|---|
| skani | 0.468% | FastANI | 15 对真实口腔/肠道基因组，85–95% |
| Syn2bANI mash_ani | 2.808% | FastANI | 同上 |
| Syn2bANI chained_kmer_ani (v7) | 2.971% (r = −0.107) | FastANI | 同上 |
| Syn2bANI −0.028 经验校准 | 1.031% | FastANI | 同上 |
| Syn2bANI GBRT v7 | 1.142% | FastANI | 同上 |
| **Syn2bANI MLE (v8)** | **0.053%** | **构造真值** | 12 个仿真基因组，85–99.9% |

两点必须在论文里说清楚：

1. v8 的数字来自**仿真**，v7 的数字来自**真实基因组 + FastANI 当真值**。
   在真实数据上重跑 v8 是下一步的首要任务。
2. FastANI 在 < 92% ANI 区间本身可靠性有争议（skani 论文即以此为主要批评点），
   拿它当真值又和 skani 比较，方法学上有偏。真实数据验证应改用
   nucmer/MUMmer（ANIm）或 minimap2 比对得到的真值。

另外，v7 的 `r = −0.107` 不应解释为"block 只覆盖保守区所以系统性偏高"：
**系统偏差会保留相关性**，`r ≈ 0` 说明区间本身算错了。见 §5。

---

## 3.6 真实基因组：速率异质性（本轮最重要的发现）

前面所有仿真用的都是**均匀**突变率，所以在原理上不可能发现真实基因组最容易违反的
那条假设。

在 13 条完整肠杆菌科染色体上对比 skani 0.1.0 和 FastANI 1.34（参考为
*E. coli* K-12 MG1655，可用代码仓库 `prototype/realgenome_bench.sh` 复现），
单速率拟合**系统性偏高，而且偏差随分歧度增长**：99% ANI 处 +0.4、97% 处 +0.7、
92% 处 +2.3、82% 处 +5。

原因在诊断列里已经写着了。每一对的 `ani_from_hist` 都高于 `ani_from_loss`，
gap 从 0.33 一路涨到 7.9。**gap 与"和参考工具的偏差"的相关系数
r = 0.957（skani）/ 0.967（FastANI）**。也就是说，那个一致性检查从来不是在
检测噪音，它一直在测量基因组的一个真实性质。

真实基因组对是马赛克：受纯化选择的保守核心，加上分歧大得多的区段。tag 优先在
保守区存活，于是存活 tag 的 mismatch 直方图比丢失率所暗示的更紧，而假设单一速率的
拟合落在两个信号之间，偏高。

### 模型

给每个区域一个速率乘子 `r ~ Gamma(α, α)`。速率变异是**区域尺度**（kb）的，
而一条 tag 只有约 30 bp，所以一条 tag 内所有位点共享同一个 `r`。对 `r` 积分之后，
每条 tag 的 mismatch 计数从 Poisson 变成**负二项**：

```
ln P(m) = m·ln(b·d) − ln m! + α·ln α − lnΓ(α) + lnΓ(α+m) − (α+m)·ln(α + d·k)
ANI     = (1 + d/α)^(−α)
```

这里的 ANI 是 gamma 混合后的平均同一性——即期望的相同碱基比例，正是比对类方法
测量的量。两个参数（平均分歧 d、形状 α）对三个自由度（m=0,1,2 和 miss），
恰好可识别。α→∞ 退回均匀模型。

两个护栏都是踩坑踩出来的：

- **似然比门控**：近乎相同的基因组几乎全是零 mismatch 的 tag、丢失率极低，
  α 根本不可识别，无约束拟合会跑到边界并过度校正（W3110 读成 99.70 而不是 99.93）。
  只在 `2·ln(L_het/L_unif) > 3.841` 时才花掉第二个参数。
- **α 限制在 [0.1, 200]**：真实替换速率变异拟合出的 gamma 形状约在 0.1–2；
  低于这个范围说明优化器在边界上，不是在信号上。

### 结果（skani 同样愿意报告的 8 对）

| 基因组 | 异质 | 均匀 | skani | FastANI | α | retention |
|---|---|---|---|---|---|---|
| E. coli W3110 | 99.90 | 99.93 | 99.99 | 99.98 | 0.07 | 1.00 |
| E. coli BL21(DE3) | 99.10 | 99.38 | 98.97 | 99.00 | 0.21 | 0.96 |
| E. coli O157:H7 Sakai | 98.11 | 98.52 | 98.08 | 97.76 | 0.65 | 0.91 |
| S. flexneri 301 | 97.78 | 98.51 | 98.06 | 97.75 | 0.43 | 0.91 |
| S. sonnei Ss046 | 97.61 | 98.61 | 98.26 | 98.08 | 0.31 | 0.91 |
| E. coli CFT073 | 97.05 | 97.60 | 96.93 | 96.69 | 1.14 | 0.84 |
| E. coli UTI89 | 97.04 | 97.57 | 96.94 | 96.70 | 1.22 | 0.84 |
| E. fergusonii | 90.86 | 94.24 | 91.97 | 91.18 | 1.00 | 0.57 |

| | 偏差 | MAE |
|---|---|---|
| vs skani，异质 | −0.049 | **0.094** |
| vs skani，均匀 | +0.706 | 0.707 |
| vs FastANI，异质 | +0.207 | 0.207 |
| vs FastANI，均匀 | +0.962 | 0.962 |

（上表数值为修正酶 panel 后的最终结果，见 §3.7。W3110 处似然比门控正确拒绝了
第二个参数，`shape` 显示 `uniform`，估计值与 skani 完全一致。）

### 检出下限

剩下 5 对（*Salmonella* ×2、*Citrobacter*、*Klebsiella*、*Enterobacter*）
retention 只有 0.13–0.20、AF 0.13–0.33。两个速率模型在那里都在外推——异质拟合
过度校正到 75–80，而 FastANI 报 81–82——所以它们被标记为
`BELOW_DETECTION`（retention < 0.20）而不是给出数字。**skani 拒绝报告的正是同样这 5 对。**

### 这修正了 §1 的定性

似然修掉的是**统计**缺陷（截断、accessory 混淆、多酶 k），而速率异质性是
**生物学上的模型错配**。这正是 GBRT 一直在经验性吸收的东西。显式建模能拿到同样的
校正，但代价是两个可解释参数加一个显著性检验，而不是 728 对训练数据——不过
我最初"完全不需要校准"的说法**过强了，应当按此修正**。

---

## 3.7 碎片化装配，以及一个仿真原理上抓不到的 bug

把基因组切成 contig 不改变序列，所以对任何参考的真实 ANI **精确不变**，
任何随 N50 的漂移都是纯伪影。`prototype/fragment.py` 还会把约一半 contig
反向互补并打乱顺序，模拟真实装配。

### bug：tag 长度超过打包宽度

把 *E. coli* O157:H7 Sakai 和**它自己的反向互补**比——序列完全相同，
答案必须是 100%——结果返回 **98.81%**，丢了 7.3% 的 anchor。
一个 2×2 对照干净地分离了原因：打乱 contig 顺序**零影响**（输出逐位相同），
翻转方向造成了全部损失。

根因：**CspCI 的 tag 是 33 bp，而 2-bit 打包只放得下 32。**
把 33-mer 截断成前 32 个碱基不是反向互补对称的——正向链读到的 tag 保留碱基
0–31，而同一位点从另一条链读到的保留的是对应碱基 1–32，canonical 形式对不上，
tag 无法匹配。CspCI 在反向方向丢了 **91% 的 anchor**（728 → 62）。

**仿真在原理上不可能抓到这个 bug**：仿真里两个基因组同向，不对称永远不触发。
必须用反向互补对照，或者真实 draft assembly。

`resolve_enzymes` 现在会把 `tag_length > 32` 的酶带警告剔除，而不是静默错配。
支持更长的 tag 需要把打包扩到超过一个 `u64`。修复后反向互补对照返回
**99.9999%**，随机 contig 方向在每个碎片化档位上只改变 ≤ 0.01 ANI。

默认 panel 改为 `BcgI,AlfI,AloI,FalI`。BsaXI 也去掉了——它在两个基准上都拖累精度
（仿真 MAE 0.113% → 0.074%，真实基因组对 skani 的 MAE 0.177 → 0.094）。

### 碎片化鲁棒性（Sakai vs K-12，真实 ANI 恒定）

| 装配 | contigs | ANI | AF | 仅正向 ANI |
|---|---|---|---|---|
| 完整 | 1 | 98.05 | 0.764 | 98.05 |
| N50 500 kb | 12 | 98.09 | 0.759 | 98.09 |
| N50 200 kb | 25 | 98.08 | 0.752 | 98.08 |
| N50 100 kb | 53 | 98.12 | 0.742 | 98.12 |
| N50 50 kb | 112 | 98.19 | 0.722 | 98.19 |
| N50 20 kb | 269 | 98.34 | 0.660 | 98.34 |
| N50 10 kb | 552 | 98.54 | 0.580 | 98.54 |
| N50 5 kb | 1110 | 98.71 | 0.385 | 98.70 |

到 5 kb N50 的漂移是 **+0.65**，而同一批文件上 skani 是 0.38、FastANI 是 0.35。

**所以 README 和论文里"对极端碎片化鲁棒"这个卖点在当前实现下不成立**——
20 kb N50 以下反而**比 k-mer 方法更差**，而且方向是偏高。原因是结构性的：
一条链需要在**同一条 contig 内**有 `min_chain_anchors`（4）个 anchor，
而 5 kb 的 contig 只有约 7 个 tag，凑不成链的 contig 会被整体丢弃——
丢掉锚定差的那些就是对其余的选择，ANI 因此偏高。AF 如实反映了损失
（5 kb 处 0.385），所以诊断信号是在的，只是点估计退化。

**论文影响**：这个卖点必须重写。诚实的表述是"到 ~20 kb N50 漂移很小
（≤ 0.15），更碎则退化且 AF 会如实告警"，而不是"对极端碎片化鲁棒"。
如果想真正拿回这个卖点，方向是让 `min_chain_anchors` 随 contig 长度自适应。

另外还修了一个 AF bug：AF 原来把不同 contig 的 chain span 放在同一坐标空间里合并，
而 tag 位置是 contig 局部的，于是所有 contig 的 span 都在 0 附近重叠、塌成一段。
12 条 contig 的装配 AF 读成 0.12 而不是 0.76，20 kb N50 以下读成 0.00。
现在按 contig 分别合并。

---

## 3.8 真实 draft assembly：canonical 修复的验证，以及 AF 的短板

§3.7 是模拟切分（保留每一个碱基、切点随机）。真实 draft 还多了重复区边界的序列丢失、
污染，以及由覆盖度和重复结构决定的 contig 长度分布。
`prototype/draft_bench.sh` 从 ENA 下载 8 个真实 contig 级 *E. coli* 装配，
从 88 到 8025 条 contig——最后那个 8.9 Mb，约两倍 E. coli 基因组，
是污染/混装样本，即一个真实的糟糕 MAG。

### strand-canonical 哈希在真实装配上的验证

把真实 draft 的**每一条 contig 都反向互补**。序列内容不变，所以 ANI 必须是 100%。
真实 contig 方向本来就是任意的，所以这才是 canonicalization 真正要应对的场景，
而模拟切分测不出来。

| 装配 | contigs | N50 | vs 自身 revcomp 的 ANI | AF |
|---|---|---|---|---|
| GCA_001283865 | 88 | 281,913 | 99.9999 | 0.970 |
| GCA_001077875 | 143 | 220,684 | 99.9999 | 0.962 |
| GCA_001284645 | 95 | 204,010 | 99.9999 | 0.964 |
| GCA_001283245 | 121 | 148,353 | 99.9999 | 0.959 |
| GCA_001283605 | 162 | 135,212 | 99.9999 | 0.944 |
| GCA_001284145 | 137 | 122,945 | 99.9999 | 0.953 |
| GCA_001283205 | 225 | 108,563 | 99.9999 | 0.935 |
| GCA_001075925 | 8025 | 3,941 | 99.9999 | 0.450 |

**8 个全过**，包括那个 8025 条 contig 全部翻转的样本。这条修复的验证补齐了。

### 对 K-12 的三方对比

| 装配 | contigs | N50 | syn2bani | 均匀 | skani | FastANI | AF_syn | AF_sk | AF_fa |
|---|---|---|---|---|---|---|---|---|---|
| GCA_001283865 | 88 | 281,913 | 97.10 | 97.60 | 96.48 | 96.69 | 0.82 | 0.79 | 0.85 |
| GCA_001077875 | 143 | 220,684 | 98.51 | 98.92 | 98.48 | 98.43 | 0.86 | 0.86 | 0.89 |
| GCA_001284645 | 95 | 204,010 | 97.14 | 97.63 | 96.98 | 96.60 | 0.77 | 0.74 | 0.80 |
| GCA_001283245 | 121 | 148,353 | 98.57 | 98.91 | 98.31 | 98.34 | 0.82 | 0.82 | 0.85 |
| GCA_001283605 | 162 | 135,212 | 98.56 | 98.92 | 98.32 | 98.27 | 0.81 | 0.81 | 0.85 |
| GCA_001284145 | 137 | 122,945 | 98.54 | 98.91 | 98.41 | 98.34 | 0.80 | 0.81 | 0.84 |
| GCA_001283205 | 225 | 108,563 | 98.60 | 98.93 | 98.28 | 98.30 | 0.76 | 0.77 | 0.82 |
| GCA_001075925 | 8025 | 3,941 | 97.92 | 98.14 | 97.01 | 97.02 | 0.33 | 0.54 | 0.82 |

| | 偏差 | MAE |
|---|---|---|
| vs skani，异质 | +0.334 | 0.334 |
| vs skani，均匀 | +0.711 | 0.711 |
| vs FastANI，异质 | +0.369 | 0.369 |
| vs FastANI，均匀 | +0.746 | 0.746 |

偏高的量**随碎片化增长，和 §3.7 用仿真预测的完全一致**：
N50 221 kb 处 +0.03，108–148 kb 带 +0.26 到 +0.32，8025 contig 处 **+0.91**。
好装配上 ANI 和两个参考工具都在 0.3 以内。

### AF 在严重碎片化装配上的短板，以及什么修不了它

8025 contig 那个装配 AF 只有 **0.33**，而 skani 是 0.54、FastANI 是 0.82。

`min_chain_anchors` 是最直觉的嫌疑，实测**不是**原因——从 4 降到 2，
AF 只从 0.328 挪到 0.350：

| `min_chain_anchors` | AF (N50 3.9 kb) | chains | AF (N50 221 kb) |
|---|---|---|---|
| 2 | 0.350 | 526 | 0.861 |
| 3 | 0.339 | 418 | 0.859 |
| 4 | 0.328 | 355 | 0.858 |
| 6 | 0.294 | 269 | 0.858 |

真正的原因是 **AF 的定义**：一条链的跨度是首 anchor 到末 anchor，
所以两端各短了约一个 tag 间距；而 anchor 少于 2 个的 contig 完全不贡献。
N50 3.9 kb、tag 间距约 1.1 kb 时，一条 contig 只有约 3.5 个 tag，
两项损失都很大，而且有 **16,050 个 contig 端**要损失——完整基因组只有 2 个。

要修就得把孤立 anchor 也计入 AF，或者把链跨度按有界的量向 contig 两端延伸，
两者都要和"在真正非同源的 contig 端虚报 AF"权衡。本轮未做。

**论文影响**：AF 这个短板必须在方法局限里写明，尤其因为 AF 恰恰是我们主打的
"能诚实告警"卖点的载体。好消息是它偏保守（低报而非高报），所以用 AF 做筛选阈值时
是安全的一侧。

---

## 4. 尚未验证（论文不能声称的部分）

- **没测 indel**。梯度实验只有替换突变加一个 400 kb 倒位；`simulate.py` 支持
  `indel_rate` 但本次跑的是 0，所以 gap 算术路径未被压测。
- **accessory 是 shuffle 造的**：保留碱基组成、彻底破坏同源性，但真实 accessory
  基因 GC 不同，且可能带部分旁系同源。
- **只用了一个物种**（*E. coli* K-12 MG1655）。GC 含量和重复结构会变，
  高 GC / 低 GC 物种未测。
- **真实数据只有 13 对完整基因组 + 8 个真实 draft、一个参考、一个科**
  （§3.6、§3.8）。没有高 GC 或低 GC 类群。
- **没有真正来自宏基因组的 MAG**：§3.8 用的是分离菌株的 draft 装配，
  真正的 MAG 还多了分箱错误和跨物种污染。
- **AF 在严重碎片化装配上明显低报**（§3.8），原因已定位但未修。
- **一致 ≠ 准确**：§3.6 比的是 skani 和 FastANI，它们本身也是估计值，而
  FastANI 在 < 92% ANI 的可靠性有争议。更强的检验需要 ANIm（nucmer）或
  minimap2 得到的参考值。
- **~85% ANI 以下**期望存留率跌破 0.20，一致性交叉检查自动关闭；联合拟合仍然工作，
  但少了一层质控。
- **小于几个 tag 间距的真实 accessory 区段**（比如只含 3 个 tag 的 2 kb 插入）
  低于 `max_skip` 下限 5，仍然会被跨过。等长的才有这个问题——变长的由
  gap penalty 拦住。
- §3.2 残留的约 +0.1 均匀偏置来源未查明（链边界删失已被 §3.3 排除），
  `mle.rs` 里的边缘校正是候选方向。

---

## 5. v7 代码中发现的缺陷（记录用；`dist` 路径仍然存在这些问题）

审阅 HEAD `5148d88` 时发现。`ani` 子命令是新增路径，不含这些问题。

| 位置 | 缺陷 |
|---|---|
| `ani_calculator.rs::gbrt_debias_ani` | 用 `raw_ani` 伪造 `shared` 和 `containment`（`shared = raw_ani × total.min()`）。因为 `raw_ani ≈ 0.95` 恒定，`containment` 实际是常数，零信息量。真实 shared 数就在旁边未被使用。 |
| `tag_matcher.rs` near-match fallback | 扫描**全部**参考 tag 找全局最小 Hamming，无任何位置约束——O(n²)，而且会配对位置完全无关的 tag。 |
| `synteny_builder.rs::sparse_chain` | 重建出 path 后只保留首尾，再把整段连续切片 `indices[start..=end]` 交给 `create_block`，把 DP 刚拒绝的非共线 anchor 全部重新放行。剩余 anchor 还被逐个发射为单点 block。 |
| `synteny_builder.rs::sparse_chain` | 只有 `gap_diff ≤ INDEL_TOL`，**没有 max_gap**，一条链可横跨整个基因组，于是"chain 内"实际等于全基因组。与上一条叠加，是 `chained_kmer_ani` 得到 r = −0.107 的最可能原因。 |
| `tag_extractor.rs` | tag 从未 canonical 化，倒位段和反向 draft contig（约占任意 draft assembly 的一半）的 tag 完全无法匹配。已由 `canonical_packed` 修复。 |
| v7 `TagSet.sequences` | 为算 `chained_kmer_ani` 而携带原始 contig 序列，使方法**必须有完整装配**才能运行，丧失了在真实 2bRAD 测序数据上的适用性。`ani` 路径只用 tag，保住了这个性质。 |

---

## 6. 论文写作影响

- **核心卖点应改为"闭式统计模型 + 内建质控"**，而不是"更好的校准"。
  卖点是：不需要训练数据、不需要参考数据库、换酶 panel 不用重训、
  每个估计自带标准误、异质性形状和检出标记。这比"MAE 比 skani 低"更难被质疑。
- **一致性诊断预测偏差（r = 0.96）是一个很强的图**：它证明这个方法能
  *自己察觉* 自己何时不可信，而 containment 类方法做不到。建议做成散点图，
  x 轴 `ani_from_hist − ani_from_loss`，y 轴与 skani 的差值。
- **拟合出的 α 本身有生物学含义**：0.07（K-12 内部菌株）→ 0.3–0.4（Shigella）
  → 1.1–1.2（E. coli 致病株）→ 1.0（E. fergusonii）。这是一个免费的副产品，
  度量两个基因组分歧的"马赛克程度"，可以独立于 ANI 讨论。
- **识别位点作为不可消除的截断机制**（`a^s` 上限）是一个新的、可发表的定量结论：
  它解析地给出了 2bRAD 类方法在低 ANI 端的天花板，与 mismatch 预算无关。
- **AF 与 ANI 解耦**可以画成图 3.2 那张表的形式（真值恒定、accessory 变化），
  这是对 containment 类方法最直观的对照实验。
- **GBRT 章节需要重写或降级**为"我们最初尝试的路线及其为何不可行"。
  v2–v7 的模型文件保留在仓库中作为对照。
